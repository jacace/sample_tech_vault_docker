Project to store basic HashiCorp vault configuration for development

Pending: 
#1 Certificates mounted in volume: openssl req -newkey rsa:2048 -new -nodes -x509 -days 3650 -keyout key.pem -out cert.pem

